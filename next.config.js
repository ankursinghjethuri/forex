/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true
  },
  basePath: '/forex',
  assetPrefix: '/forex/'
}

module.exports = nextConfig
